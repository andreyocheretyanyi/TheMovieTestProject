package ua.codeasylum.themovietestproject.model

import androidx.lifecycle.AndroidViewModel
import ua.codeasylum.themovietestproject.App
import javax.inject.Inject

class SearchViewModel @Inject constructor(app: App) : AndroidViewModel(app) {
}